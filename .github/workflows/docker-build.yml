name: Build and Push Docker Image

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Separate image name so this combined branch doesn't clobber
          # either parent's published image. RunPod templates for this
          # branch should point at
          #   <DOCKERHUB_USERNAME>/ltx-video-fa3-teacache:latest
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ltx-video-fa3-teacache:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ltx-video-fa3-teacache:${{ github.sha }}
          no-cache: true
